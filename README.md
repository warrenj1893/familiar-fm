# familiar.fm

Summerfest Lineup Redesign — a standalone interactive web app.

## Local Development

```bash
npx serve . -p 3000
```

Then open [http://localhost:3000](http://localhost:3000)

## Deploy to Vercel

```bash
npx vercel
```

Or connect the GitHub repo to Vercel — no build step required (static site).
